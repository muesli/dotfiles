#!/bin/bash

PACKAGES=("${PACKAGES[@]}"
    # IDEs / Code editors
    "code"
    # CMake
    "cmake" "extra-cmake-modules"
    # SCMs
    "git"
    # Go
    "go" "go-tools"
    # Web-dev on CLI
    "httpie"
)
